# Road Viewer for Home Assistant

Openpilot 주행 로그와 전방 카메라 영상을 업로드하고, **영상·주행 상황·차량 정보 그래프를 같은 시간축으로 확인하는 Home Assistant 앱**입니다.

## 주요 기능

- 여러 구간의 로그·영상 업로드, 중복 로그 건너뛰기, 로그에 영상 나중에 추가
- 자동 또는 수동 변환, 최대 4개 동시 처리, 처리 단계·진행률 표시
- 자동 배치·좌우 분할·상하 분리, 화면 폭 조절 팝업
- 차선·도로 경계·모델 주행 경로·앞차·레이더 감지점 표시와 영상 겹쳐보기
- 차량 정보 그래프 20종, 표시 항목 선택·드래그 정렬·순서 초기화
- 카메라 보정 각도·높이·디바이스 ID 확인
- 원본 TS 보관 여부 선택, TS 또는 MP4 다운로드
- 보관 파일·변환 데이터 용량 확인, 변환 데이터 제거·완전 삭제·미완료 업로드 정리
- Home Assistant 로그인과 Ingress를 통한 접근
- 선택적 HTTPS 장치 Push 업로드, 페어링·HMAC 인증과 연결 해제
- 저장공간 한도와 용량 예약, 오래된 미고정 주행 자동 삭제·Pin

## 설치와 시작

1. Home Assistant의 **설정 → 앱 → 앱 스토어 → ⋮ → 저장소**에서 `https://github.com/Qualified4/roadviewer-ha`를 추가합니다. 버전에 따라 앱이 ‘애드온’으로 표시됩니다.
2. **Road Viewer**를 설치하고 시작한 뒤 **웹 UI 열기**를 누릅니다. 사이드바 표시도 켤 수 있습니다.
3. **파일 추가**로 `rlog.zst`와 같은 구간의 `qcamera.ts`를 선택하고 **업로드**합니다.
4. 변환이 끝나면 목록의 **재생**을 누릅니다. 자동 변환을 껐다면 먼저 **변환**을 누릅니다.

로그만 업로드해도 주행 상황과 차량 정보를 볼 수 있습니다. 영상만 단독으로 등록할 수는 없습니다. 다음과 같은 여러 구간의 파일명도 지원합니다.

```text
0000035f--b513269850--0--rlog.zst
0000035f--b513269850--0--qcamera.ts
0000035f--b513269850--1--rlog.zst
0000035f--b513269850--1--qcamera.ts
```

**HACS가 아닌 앱 스토어에 추가하는 저장소입니다.** 별도의 Road Viewer 포트를 개방할 필요 없이 기존 Home Assistant 원격 접속을 통해 사용할 수 있습니다.

## 저장 및 업데이트

원본 로그·영상과 변환 결과는 앱 내부 `/data/roadviewer`에 저장됩니다. **이 데이터는 Home Assistant 앱 백업에서 제외되므로, 보관할 원본은 별도로 유지하세요.** 앱 설정과 처리 설정은 백업에 포함되고, 화면 표시 설정은 브라우저에 저장됩니다.

업데이트에서 분석 형식이 바뀌면 기존 변환 결과를 지우고 원본으로 다시 처리할 수 있습니다. 모든 버전 업데이트가 재변환을 요구하는 것은 아닙니다.

업로드 제한, 변환·삭제 동작, 그래프 해석과 문제 해결은 [설치 및 사용 설명](roadviewer/DOCS.md), 버전별 변경은 [변경 이력](roadviewer/CHANGELOG.md)을 참고하세요.

## 개발 및 검증

`roadviewer/`가 Docker 빌드 컨텍스트입니다. GitHub Actions에는 amd64/aarch64 이미지 빌드, 이미지 메타데이터·API·로그 및 영상 처리 검사와 Playwright 브라우저 테스트가 구성되어 있습니다. 브라우저 테스트는 업로드, 재생, 그래프, 팝업, 설정 저장, 반응형 배치 등을 확인합니다. CI 검증이 모든 Home Assistant 실기기와 Android 파일 선택창 동작을 보장하지는 않습니다.

사용자 로그·영상은 저장소에 포함하지 않습니다. 포함된 openpilot 스키마와 UI 리소스의 라이선스는 [OPENPILOT-LICENSE](roadviewer/schema/OPENPILOT-LICENSE)를 참고하세요.

## 외부 장치 업로드와 저장공간 관리

기존 웹 UI는 계속 Home Assistant Ingress를 사용합니다. 장치 API는 기본적으로 꺼져 있으며, `/ssl` 인증서를 준비하고 애드온의 8443/tcp 호스트 포트를 지정하면 켜집니다. 별도 API 스위치 없이 포트 입력/비우기로 켜고 끄며 변경 후 재시작합니다. Nginx가 HTTPS를 종료하고 장치 API만 공개합니다. WAN 포트는 호스트 포트와 달라도 됩니다.

변환 및 저장 설정에서 **저장공간 관리**, 로그 업로드에서 **외부 장치 연결**을 펼칠 수 있습니다. 저장공간은 기본 무제한이며, 부족 시 업로드 거부 또는 오래된 미고정 주행 삭제를 선택합니다. 로그 추가 작업 메뉴의 Pin/Unpin은 같은 주행의 모든 구간에 적용됩니다. 한도는 업로드 사전 검사와 예약 기준이며, 이후 변환 결과 크기까지 강제하는 파일시스템 quota는 아닙니다.

설치 절차와 API 계약, HMAC 서명 예제, 이어올리기 및 오류 처리는 [Device API v1 문서](docs/device-api.md)를 참고하세요. openpilot 쪽 업로더 구현은 별도입니다.
