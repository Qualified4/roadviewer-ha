# Road Viewer for Home Assistant

Openpilot 주행 로그와 전방 카메라 영상을 올리고, **영상·주행 상황·차량 정보 그래프를 같은 시간축으로 확인하는 Home Assistant 앱**입니다.

## 시작하기

1. Home Assistant의 **설정 → 앱 → 앱 스토어 → ⋮ → 저장소**에서 `https://github.com/Qualified4/roadviewer-ha`를 추가합니다. 버전에 따라 ‘앱’이 ‘애드온’으로 표시됩니다.
2. **Road Viewer**를 설치하고 시작한 뒤 **웹 UI 열기**를 누릅니다.
3. **파일 추가** 또는 업로드 영역의 드래그 앤 드롭으로 파일을 선택하고 **업로드**합니다.
4. 변환이 끝나면 **재생**을 누릅니다. 자동 변환을 껐다면 목록에서 먼저 **변환**을 누릅니다.

**HACS용 통합이 아닌 앱 스토어 저장소입니다.** 웹 UI는 Home Assistant Ingress를 사용하므로 별도 외부 포트를 열 필요가 없습니다. 원격에서도 기존 Home Assistant 주소로 접속합니다.

## 지원 플랫폼과 설치 방식

| Home Assistant 플랫폼 | 이미지 플랫폼 | 대상 |
|---|---|---|
| `amd64` | `linux/amd64` | Intel/AMD 64비트 PC·미니 PC·서버 |
| `aarch64` | `linux/arm64` | ARM 64비트 Home Assistant 기기 |

설치 시 GHCR의 프리빌드 이미지 중 **기기에 맞는 하나만 다운로드**합니다. 플랫폼별 이미지를 함께 배포해도 사용자의 다운로드 용량이 합산되지 않습니다. 32비트 ARM·i386은 지원하지 않습니다.

기기에서 패키지를 설치하고 이미지를 빌드하는 과정을 줄이며, Home Assistant의 다운로드·압축 해제 진행률을 활용합니다. 표시 방식은 Home Assistant/Supervisor 버전에 따라 다르고, 전체 설치 시간의 정확한 백분율을 의미하지는 않습니다.

## 업로드할 파일

같은 구간의 `rlog.zst`와 `qcamera.ts`를 함께 선택합니다. 여러 구간은 다음처럼 원래 식별자가 포함된 이름으로 올릴 수 있습니다.

```text
00000395--0d0eda17c5--7--rlog.zst
00000395--0d0eda17c5--7--qcamera.ts
00000395--0d0eda17c5--8--rlog.zst
00000395--0d0eda17c5--8--qcamera.ts
```

- 로그만 올려도 주행 상황과 차량 정보를 볼 수 있습니다. 영상만 단독으로 등록하거나 MP4를 직접 업로드하는 기능은 지원하지 않습니다.
- 영상은 나중에 같은 로그와 함께 올려 기존 항목에 추가할 수 있습니다.
- TS가 삭제되고 MP4만 남은 항목은 같은 로그와 원본 TS를 다시 올리면 해시를 확인해 TS만 복원합니다. 기존 MP4와 분석 결과는 유지합니다.
- 중복 로그는 파일 내용의 SHA-256으로 판정합니다. 전송 후 등록 단계에서 중복을 건너뜁니다.
- 기본 512 MB는 **업로드 한 번의 파일 합계 제한**입니다. 전체 저장 한도는 별도로 설정합니다.

## 재생과 분석

| 기능 | 사용 방법 |
|---|---|
| 함께 보기 | 영상과 주행 상황 또는 차량 정보 그래프를 동기 재생합니다. |
| 화면 배치 | 자동 배치·좌우 분할·상하 분리, 모바일·태블릿·PC별 화면 폭 조절을 지원합니다. |
| 주행 상황 | 차선·도로 경계·모델 경로·앞차·레이더 감지점을 표시하고 영상에 겹쳐 볼 수 있습니다. |
| 차량 정보 | 그래프 20종 중 표시할 항목을 고르고 드래그해 순서를 바꿉니다. |
| 로그 이동·고정 | 같은 주행의 구간 선택과 이전·다음 로그 이동, 구간별 핀 고정을 지원합니다. |
| 스크롤 제어 | 상단 탐색 영역과 하단 재생바를 유지합니다. 상단 제목 줄의 화살표로 탐색 버튼을 접고 펼칩니다. |

화면 배치·표시 항목·그래프 순서·접힘 상태는 사용 중인 브라우저에 저장됩니다. 재생 화면의 핀과 로그 목록의 핀은 같은 구간별 고정 설정입니다.

## 변환과 파일 관리

**변환 및 저장 설정**에서 자동 변환, 동시 처리 개수(1~4개), 원본 TS 보관 여부를 선택합니다. 처리 단계와 가능한 진행 정보를 표시하며, 원본 TS 보관을 끄면 MP4 생성·검증에 성공한 뒤 TS를 삭제합니다. 영상 다운로드는 TS가 있으면 TS, 없으면 보관 중인 MP4를 제공합니다.

| 작업 | 남는 파일 |
|---|---|
| 제거 | 원본 로그·TS, 또는 TS 없이 보관하는 유일한 MP4를 유지합니다. 분석·그래프와 다시 만들 수 있는 MP4를 삭제합니다. |
| 완전 삭제 | 해당 구간의 원본·영상·변환 결과를 모두 삭제합니다. |
| 저장소 즉시 정리 | 등록된 로그를 유지하고 미완료 브라우저 업로드의 임시 파일을 정리합니다. |

여러 항목은 설정 맨 아래 **로그 선택**에서 한 번에 변환·제거·완전 삭제할 수 있습니다. 고정된 구간은 일괄 제거·완전 삭제에서 기본적으로 제외됩니다. **고정 항목도 삭제**를 켜면 포함되며, 팝업을 다시 열면 꺼집니다. 개별 로그 메뉴에서는 고정 여부와 관계없이 직접 제거·완전 삭제할 수 있습니다.

**저장공간 관리**에서는 한도와 공간 부족 시 정책을 설정합니다. 기존 로그를 유지하며 업로드를 거부하거나 오래된 미고정 구간을 삭제할 수 있습니다. 이 한도는 업로드 사전 검사 기준이며 변환 결과까지 강제하는 파일시스템 할당량은 아닙니다.

## 저장·백업·업데이트

데이터는 앱 컨테이너의 `/data/roadviewer`에 저장됩니다. **원본 로그·영상·변환 결과는 Home Assistant 앱 백업에서 제외**되므로 보관할 원본은 별도로 유지하세요. 처리·저장 정책·장치 인증 설정은 백업에 포함됩니다.

업데이트에서 분석 형식이 바뀌면 기존 변환 결과를 정리하고 다시 처리할 수 있습니다. 모든 업데이트가 재변환을 요구하는 것은 아닙니다.

## 외부 장치 연결

장치에서 직접 업로드하려면 **로그 업로드 → 외부 장치 연결**에서 HTTPS 연결을 켜고 포트를 저장한 뒤 **지금 재시작**으로 적용합니다. 유효한 `/ssl` 인증서가 필요하며 기본값은 꺼짐입니다. 웹 UI 접속에는 이 설정이 필요하지 않습니다.

페어링 팝업에서 코드 복사·남은 시간 확인·새 코드 발급을 할 수 있습니다. 코드는 5분간 한 번 사용할 수 있고 재발급하면 이전 코드는 무효가 됩니다. 장치 API는 HMAC 인증과 조각 전송·이어올리기를 지원합니다.

## 문서와 검증

- [설치 및 사용 설명](roadviewer/DOCS.md): 업로드, 변환, 재생, 일괄 관리, 저장 정책, 백업과 문제 해결
- [변경 이력](roadviewer/CHANGELOG.md): 버전별 변경 내용
- [Device API v1](docs/device-api.md): HTTPS 구성, 페어링·서명, 업로드·이어올리기와 openpilot 없이 테스트하는 방법

`roadviewer/`가 Docker 빌드 컨텍스트입니다. GitHub Actions에서 amd64/aarch64 이미지 빌드와 API·변환·브라우저 검사를 실행합니다. 브라우저 검사는 반응형 배치, 설정 저장, 마우스·터치 피드백 등을 확인하며 실제 Home Assistant 기기와 Android 파일 선택창은 환경에 따라 다를 수 있습니다.

### 프리빌드 배포 순서

버전은 `roadviewer/config.yaml`에서 읽습니다. 모든 브라우저·API 테스트와 두 플랫폼의 컨테이너 검사를 통과해야 `ghcr.io/qualified4/roadviewer-ha:<버전>` 멀티플랫폼 태그를 발행합니다. PR과 일반 브랜치 push는 검사만 수행하고, `main` push 또는 원본 저장소의 수동 실행에서 이미지를 게시합니다.

1. 릴리스 브랜치에서 앱 버전·Dockerfile 기본 버전·화면 버전·변경 이력을 함께 갱신합니다. 이미 배포한 버전은 재사용하지 않습니다.
2. 해당 브랜치를 push하고 **Actions → Build, test and publish app → Run workflow**에서 그 브랜치를 선택해 이미지를 먼저 배포합니다. 최초 전환 때는 기존 이름인 **Build and test app**으로 보일 수 있습니다. 기존 워크플로가 수동 실행을 지원하므로 수정한 브랜치를 선택해 실행하면 됩니다.
3. 최초 GHCR 패키지는 비공개일 수 있습니다. GitHub의 **Packages → roadviewer-ha → Package settings → Change visibility → Public**을 확인합니다. 공개 접근 검사에서 실패했다면 공개로 변경한 뒤 실패한 `publish` 작업을 다시 실행합니다. Home Assistant에 레지스트리 자격 증명을 넣는 방식은 사용하지 않습니다.
4. 모든 작업과 익명 다운로드·플랫폼 검증이 성공한 뒤 **테스트한 커밋을 그대로 `main`에 반영**합니다. 이미지가 만들어지기 전에 `config.yaml`의 새 버전·`image:`를 `main`에 먼저 올리면 설치·업데이트가 실패할 수 있습니다.

`build-<커밋>-amd64` / `build-<커밋>-arm64` 태그는 검증한 플랫폼별 이미지이며 Home Assistant는 버전 태그를 사용합니다. 이미 있는 버전 태그는 다시 게시하지 않으므로 `main` 반영 후 재실행해도 선배포한 이미지를 유지합니다. 수정본을 배포하려면 반드시 새 버전을 사용합니다. 로컬 소스를 직접 빌드하려면 [설치 설명](roadviewer/DOCS.md#설치와-접속)의 `image:` 제거 안내를 따르세요.

Road Viewer는 [MIT 라이선스](LICENSE)로 배포합니다.

사용자 로그·영상은 저장소에 포함하지 않습니다. 포함된 openpilot 스키마와 UI 리소스의 라이선스는 [OPENPILOT-LICENSE](roadviewer/schema/OPENPILOT-LICENSE)를 참고하세요.
