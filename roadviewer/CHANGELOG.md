## 0.2.67
- 그래프 선택 팝업에서 드래그 손잡이와 위·아래 버튼으로 표시 순서를 조절하고 기억합니다. 체크 해제한 항목도 순서를 유지합니다.
- 변환 및 저장 안내를 로그 업로드 아래로 이동했습니다. 접기·펼치기는 그대로 유지합니다.
- 도로 진행 방향에 맞는 차량 뒷모습으로 웹 및 홈 어시스턴트 앱 아이콘을 변경했습니다.

## 0.2.66
- 자동 변환 토글과 설정 저장, 미변환 상태 및 접고 펼칠 수 있는 상태별 안내를 추가했습니다. 자동 변환을 꺼도 실행 중인 작업과 수동 변환은 계속 처리합니다.
- 재생성을 변환 / 제거 버튼으로 변경했습니다. 제거는 변환 데이터만 삭제하고 자동 재변환을 막으며, 완전 삭제는 원본까지 삭제합니다.
- 중복 변환 요청을 방지하고 실패 시 불완전한 변환 결과를 정리합니다.
- 주행 상황 / 차량 정보 탭, 영상과 동기화한 차량 시계열 그래프, 현재 값 요약을 추가했습니다.
- 자체 팝업에서 12종 그래프를 체크박스로 선택하고 선택 항목과 탭을 기억합니다. 그래프 목록 내부 스크롤, 시간축 확대·축소, 클릭·드래그·키보드 탐색을 지원합니다.
- 원본 메시지 시각과 샘플을 보존하며, 유효하지 않거나 오래된 데이터는 빈 구간으로 표시합니다. 그래프 데이터는 차량 정보 탭을 처음 열 때만 별도로 불러옵니다.
- 현재 재생 시점의 기기·센서·보정 상태·카메라 방향·높이를 보여주는 카메라 설치 정보 팝업을 추가했습니다. 기본 높이 사용과 확인 불가 항목을 구분합니다.
- 카메라 정보와 차량 그래프 데이터를 포함하기 위해 기존 변환 데이터는 다음 적용 시 갱신이 필요합니다. 자동 변환 설정이 켜져 있으면 처리하며, 수동 제거한 항목은 제외합니다.

## 0.2.65
- 상단바 접기 버튼의 클릭 영역을 제목·정보 행 위에 겹쳐 배치해 별도 줄과 높이를 없앴습니다. 중앙 화살표와 행 전체 클릭은 유지합니다.
- 상단바가 스크롤에 고정될 때만 접기 버튼을 표시하고, 맨 위로 돌아오면 이동 버튼을 다시 표시합니다. 독립된 고정 기준점으로 스크롤 되돌림을 방지합니다.

## 0.2.64
- 백업 제외 범위를 카메라 영상에서 기록 데이터 전체로 확대했습니다. 원본 로그·영상·변환 데이터·목록 메타데이터·업로드 임시 파일·이전 진단 로그를 제외하고 앱 설정과 동시 처리 설정은 유지합니다.
- 백업 복원 시 기록 목록은 비어 있으며, 로그와 영상을 다시 업로드하면 됩니다. 현재 저장된 기록과 기존 백업 파일은 삭제하지 않습니다.

## 0.2.63
- 모바일 업로드 조각을 256KB로 줄이고 메모리에서 동일 조각을 재시도합니다. 전송 중 목록 폴링을 쉬고 일시적인 연결 실패를 최대 5회 재시도하며 실패 단계를 앱 로그에 기록합니다.
- 미완료 업로드를 1분마다 확인하고 15분 이상 전송이 없으면 정리합니다. 같은 정리를 즉시 실행하는 버튼을 추가했습니다.
- 영상 위 차량 표식에 1m 높이 토글과 지면 연결선을 추가했습니다(기본 켜짐, 선택 기억).
- 상단바 접힘 상태와 스크롤 고정 판정을 분리하고 접기 버튼의 터치 영역을 아랫줄 전체로 넓혔습니다.
- 재생 화면 폭 조절과 선택 기억, 변환 데이터 용량 및 재생성 버튼을 추가했습니다.
- 새 Home Assistant 백업에서 원본 TS·변환 MP4·업로드 임시 파일을 제외합니다. 로그와 설정은 유지하고, 복원 후 영상이 없으면 로그 전용으로 재생성합니다.
- 높이 좌표를 추가하기 위해 기존 변환 데이터를 한 번 재생성합니다.

# 0.2.62

- 업로드 전송 대기가 변환 작업과 목록 조회를 막지 않도록 공유 잠금 밖에서 데이터를 수신합니다.
- 업로드 요청 시간 제한과 조각 재시도를 추가하고 이미 저장한 조각의 재전송을 허용합니다.
- 재시작 시 중단된 임시 업로드를 정리하고, 실행 중에는 15분간 활동이 없는 업로드를 목록 조회·새 업로드 시 정리합니다.
- 등록 실패 시 임시 파일을 정리하고 앞서 등록된 작업은 계속 처리합니다.

# 0.2.61

- 상단 접기 화살표를 제목과 세부정보 길이에 관계없이 가로 중앙에 고정합니다.

# 0.2.60

- 주행 상황 겹쳐 보기 버튼을 영상 아래로 이동합니다.
- 구간 시작 후 2초 이내에 도착한 첫 보정·센서 정보를 시작 프레임에도 적용합니다. 유효하지 않은 보정과 실제 정보 누락은 계속 표시를 제한합니다.
- 기존 로그를 재분석해 시작 프레임 오버레이를 갱신합니다.

# 0.2.59

- 상단 접기 화살표를 절반 크기로 줄이고 제목과 세부정보 사이에 배치해 별도 높이를 차지하지 않도록 합니다.

# 0.2.58

- 상단 접기 버튼을 둥근 굵은 꺾쇠 화살표로 변경하고 배경·테두리를 제거합니다.
- 버튼을 고정 영역 안쪽 하단 중앙에 배치합니다.

# 0.2.57

- 전방 영상에 주행 상황 겹쳐 보기 토글을 추가합니다. 카메라 보정값과 센서별 화각으로 도로·경로·차량 위치를 투영합니다.
- 주행 상황의 표시 선택을 반영하며 보정 정보가 없으면 안내합니다. 영상 자체에는 덧씌우지 않습니다.
- 필요한 보정 데이터를 추출하기 위해 기존 로그를 한 번 다시 처리합니다.

# 0.2.56

- 주행 상황의 좌우 표시 범위를 자동 또는 ±5/10/20/40m로 선택하고 기억합니다.
- 파일 선택 진단과 메인 화면 복귀 안내를 제거합니다.
- 스크롤 고정 영역 하단 중앙 버튼으로 이동 버튼들을 접고 펼칠 수 있습니다.

# 0.2.55

- 정보 표시 순서를 끄기, TrackID, 거리, yRel, 속도, 상대 속도, liveTracks 정보 표시로 변경합니다.
- 기본 선택은 TrackID이며 저장된 사용자 선택은 유지합니다.

# 0.2.54

- 모델 주행 경로의 기본값을 켜짐으로 변경합니다. 저장된 사용자 선택은 유지합니다.

# 0.2.53

- 새로고침 옆에서 동시 처리 개수를 1개 또는 2개로 선택할 수 있습니다. 기본값은 1개이며 서버에 저장됩니다.
- 개수를 줄여도 진행 중인 작업은 완료하고, 이후 작업부터 새 제한을 적용합니다.
- 동일 로그의 중복 실행을 방지하고 대기 작업의 순서를 유지합니다.

# 0.2.52

- 주행 상황에 기본 꺼짐인 모델 주행 경로 체크박스를 추가하고 modelV2.position을 표시합니다.
- 주행 상황 체크박스와 정보 표시 선택을 브라우저에 저장·복원합니다.
- position 추출을 위해 기존 로그의 재생 데이터를 한 번 다시 생성합니다.

# 0.2.51

- 로그 시작·끝의 0.1초 이내 시간 차이는 첫·마지막 프레임으로 표시합니다.
- 실제 로그 범위 밖에서는 시작 전·종료 후 안내를 구분하고, 무효 프레임 판정은 유지합니다.

# 0.2.50

- 영상과 로그가 함께 끝나면 마지막 영상 프레임을 유지합니다. 끝 시점의 0.1초 이내 차이를 허용합니다.
- 영상 시작 전과 영상 종료 후 로그만 남은 구간의 안내를 구분합니다.

# 0.2.49

- 구간·재생 속도·전방 거리 선택을 웹 선택창으로 통일합니다. 모바일은 하단 패널, PC는 버튼 근처 메뉴로 표시합니다.
- 현재 선택 강조, 키보드 이동·선택·닫기 및 준비되지 않은 구간의 선택 제한을 지원합니다.

# 0.2.48

- 같은 영상 ID에 여러 구간이 있으면 로그 목록 옆에 구간 선택 메뉴를 표시합니다.
- 구간 번호순으로 정렬하고 현재 구간을 선택하며, 준비되지 않은 구간은 상태를 표시하고 선택을 막습니다.

# 0.2.47

- 하단 고정 재생바 배경의 좌우 여백과 최대 너비를 상단 고정 영역에 맞춥니다.

# 0.2.46

- 앞차 거리의 '미선택' 상태 문구를 '앞차 미감지'로 변경합니다.

# 0.2.45

- '선택된 앞차' 항목 이름을 '앞차 거리'로 변경합니다.

# 0.2.44

- 상단 영역이 스크롤에 고정될 때만 재생바와 같은 패널 배경·테두리·그림자를 적용하고 버튼 위에 10px 여백을 둡니다.

# 0.2.43

- 모바일에서도 모델 프레임 수·영상 유무를 로그 제목 오른쪽에 한 줄로 표시합니다. 긴 로그 제목은 말줄임 처리합니다.

# 0.2.42

- 로그 제목·정보를 로그 이동 버튼과 함께 상단에 고정합니다.
- 차선·앞차 설명을 정보 표시 아래로 옮기고 '차량 중심 도로'를 '주행 상황'으로 변경합니다.
- 영상과 주행 상황 패널의 헤더 여백 및 좌우 분할 헤더 높이를 줄입니다.

# 0.2.41

- 상단 로그 이동 영역의 그림자를 제거하고 상단 경계의 미세한 틈으로 내용이 비치지 않도록 배경을 보완합니다.

# 0.2.40

- 재생 화면의 로그 목록·이전 로그·다음 로그 버튼이 스크롤 시 상단에 닿으면 고정되도록 변경합니다.

# 0.2.39

- 처리 진행 정보만 메모리 기반 API로 1초마다 갱신합니다. 전체 목록과 저장공간은 기존 3초 간격을 유지합니다.
- 진행률 변경 시 상태 문구만 갱신해 행과 버튼을 유지합니다.

# 0.2.38

- 좌우·상하 분할 설정을 저장하고 다음 재생 화면에서 복원하도록 되돌립니다.

# 0.2.37

- 재생 화면을 열 때 좌우·상하 분할을 모두 끄고 자동 레이아웃으로 시작합니다.

# 0.2.36

- 로그 분석 중 분석 완료 프레임 수, 영상 변환·검증 중 단계별 진행률을 표시합니다.
- 진행 정보는 최대 초당 1회 전달하고 서버 메모리에만 보관하며 기존 3초 목록 갱신을 사용합니다.

# 0.2.35

- 재생 화면의 '차량 표시' 명칭을 '정보 표시'로 변경합니다.

# 0.2.34

- 재생 바 이동 시 기존 재생/일시정지 상태를 유지합니다.

# 0.2.33

- 목록의 준비 중 상태를 굵은 강조 색상과 배경 배지로 표시

# 0.2.32

- 재시작·업데이트 재처리 대기열을 업로드가 오래된 로그부터 정렬 (목록 아래쪽부터)
- 동일 업로드 시각에도 목록·처리 순서가 일관되도록 정렬

# 0.2.31

- 영상·로그의 전체 시간 범위를 합쳐 재생, 시작 시각 차이 반영
- 영상 밖에서는 안내와 함께 로그 재생 계속, 로그 밖에서는 차량·차선 정보 숨김
- 영상 종료와 재생 타이머 분리, 마지막 영상 프레임 길이 포함

# 0.2.30

- 목록 자동 갱신 시 변경 없는 재생 버튼을 유지해 클릭·터치 중 교체 방지
- 준비 중인 재생 화면은 완료될 때까지 자동 재조회, 영상 메타데이터 로딩 후 재생 활성화
- 목록·재생 데이터 캐시 방지 및 변환별 영상 URL 적용

# 0.2.29

- 동일 로그와 영상을 다시 업로드하면 영상 없는 기존 항목에 원본 영상을 추가하고 재처리
- 영상 단독 업로드는 계속 거부, 기존 영상은 덮어쓰지 않음
- 목록 영상 유무는 qcamera.ts 존재 여부로 표시 (변환·재처리 상태와 독립)

# 0.2.28

- 상하 분리 토글 추가: 영상·레이더를 각각 본문 전체 폭으로 배치
- 좌우·상하 모드는 상호 배타적으로 선택, 둘 다 끄면 기존 자동 반응형 배치
- 보기 설정 저장 및 기존 좌우 분할 설정 승계

# 0.2.27

- 재생 속도 4배 옵션 추가

# 0.2.26

- 차량 표시 끄기 옵션 추가: 차량·감지점 도형은 유지하고 이름·정보 라벨·연결선 숨김

# 0.2.25

- 비활성화된 업로드·선택 비우기 버튼에서 기본 화살표 커서 표시

# 0.2.24

- 차량 표시 모드에 속도·상대 속도 추가 (km/h), liveTracks 정보 표시에도 적용
- 모델 속도는 leadsV3.v[0], 레이더 속도는 내 차 속도 + vRel로 계산. 데이터 없으면 — 표시

# 0.2.23

- 이전 로그·다음 로그 이동 방향 반전
- 로그 목록 버튼의 마우스 올림·누름·키보드 포커스 반응 추가

# 0.2.22

- 로그 목록 링크 오른쪽에 이전 로그·다음 로그 버튼 추가
- 목록 순서대로 재생 가능한 로그 이동, 준비 중·실패 로그는 건너뜀

# 0.2.21

- 낮은 확률 앞차 체크박스 제거, 모델 앞차는 확률과 무관하게 표시
- 확률 50% 미만인 모델 앞차의 흐린 표시 유지

# 0.2.20

- liveTracks 정보 표시 체크박스 추가: 선택한 거리·yRel·TrackID 모드를 감지점에도 적용
- 기본은 정보 숨김, liveTracks 감지점을 끄면 정보도 함께 숨김

# 0.2.19

- 우측 상단에 앱 데이터 저장공간 사용량 표시 (원본·변환·임시 파일·진단 기록 포함)
- 한 번에 업로드할 파일의 합계 용량 제한을 업로드 버튼 아래로 이동

# 0.2.18

- 재처리 대상 전체를 먼저 대기 상태로 바꾸고 기존 변환 영상·데이터 일괄 삭제 후 순차 변환
- 업로드 원본은 보존하고 재처리 중인 영상 URL의 재생 차단

# 0.2.17

- 차량 라벨을 거리·yRel·TrackID 중 하나로 선택하도록 통합
- TrackID는 기존 거리 자리에 표시하고 별도의 liveTracks ID 라벨 제거
- 선택 앞차의 radarTrackId 보존. ID 없는 대상은 — 표시

# 0.2.16

- 레이더 거리 라벨에서 yRel 접두어 제거, 숫자와 단위만 표시

# 0.2.15

- 프레임 번호를 전방 카메라 제목 아래로 이동하고 등폭 글꼴·고정 폭 제거, 속도·조향 상태 오른쪽 정렬
- yRel 선택 시 기존 전방 거리 라벨을 대체하도록 수정, liveTracks 중복 yRel 라벨 제거

# 0.2.14

- 차선 확률·로드엣지 표준편차를 원본 배열 y[0] 좌우 거리로 전환하는 토글 추가
- 왼쪽 음수·오른쪽 양수, 단위 m 표시. 원본 첫 값이 없거나 유효하지 않으면 빈 값 표시
- 기존 로그의 변환 캐시 갱신으로 원본 첫 점 보존

# 0.2.13

- 도로 화면에 yRel 표시 체크박스 추가: 레이더 차량·선택 앞차·liveTracks의 원본 횡방향 거리 표시
- 기존 거리 아래 별도 줄로 표시하며 감지점 ID와 독립적으로 선택 가능

# 0.2.12

- 프레임 번호에 고정 폭 영역·등폭 글꼴을 적용해 숫자 변경 시 속도와 조향 아이콘 흔들림 방지
- 속도 숫자의 표시 폭도 고정해 자릿수 변경 시 배치 유지

# 0.2.11

- 재생 화면에 좌우 분할 토글 추가: 영상과 레이더를 모바일에서도 나란히 표시
- 보기 설정을 기기에 저장하고 전환 중 재생 위치·속도 유지

# 0.2.10

- 파일 추가 버튼 하나로 통합하고 폴더·개별 추가 버튼 제거
- 한 개씩 반복 추가 및 다중 선택 누적 유지
- 파일 추가 버튼·선택 목록·업로드 버튼 디자인 개선

# 0.2.9

- 모바일 파일 선택 진단: 선택창 설정, 원본 반환 파일 목록, 처리 결과, 화면 전환·재시작 및 스크립트 오류 기록
- 전송 대기 기록 기기 보관, 서버 진단 파일 보관·다운로드와 앱 로그 출력
- 파일 내용과 Ingress 주소는 수집하지 않음

# 0.2.8

- 재생·이전·다음·속도·탐색 바를 재생 화면 하단에 고정
- 제어 영역 높이에 맞춰 본문 하단 여백 자동 조정
- 모바일 줄바꿈 및 하단 안전 영역 반영

# 0.2.7

- SHA-256 로그 내용 비교로 기존 저장 로그·동일 묶음·동시 요청 중복 등록 차단
- 기존 버전의 저장 파일도 자동 해시 확인
- 신규 등록·중복 건너뜀 개수 및 영상 구성 차이 안내

# 0.2.6

- 모바일용 단일 파일 추가와 명시적인 모든 파일 선택 유형
- 여러 번 선택한 로그·영상 합치기, 선택 목록·비우기 제공
- 지원하지 않는 파일 안내 및 선택창 실행 중 input 변경 제거
- 화면 버전 표시와 라이브러리 캐시 갱신
- Android 앱 화면 복귀 문제 발생 시 브라우저 업로드 안내

# 0.2.5

- 차량 속도 옆에 원본 mici 당근 핸들 아이콘 추가
- steeringPressed 운전자 조향 개입 시 흰색 우선 표시
- 조향각 회전, 토크 기반 확대·색상, 레인 및 steerRequired 표시
- 누락·오래된 조향 상태 구분 및 기존 로그 자동 재변환

# 0.2.4

- 모바일 파일 선택창의 확장자 필터 제거 (선택 후 지원 파일명 검사 유지)
- 파일 선택 취소 시 기존 선택 유지 및 같은 파일 재선택 처리

# 0.2.3

- 앱 스토어·웹 화면 아이콘 추가
- 카메라 상단에 carState.vEgo 기반 내 차량 속도(km/h) 표시
- 기존 로그 자동 재변환

# 0.2.2

- 대용량 업로드를 1MiB 조각으로 전송하여 Ingress 요청 크기 제한에 대응
- JSON이 아닌 프록시 오류 응답도 설명 메시지로 표시

# 0.2.1

- Home Assistant 이미지 버전·종류·아키텍처 라벨 추가
- CI에서 HA 이미지 메타데이터 검증 추가

# 0.2.0

- 차선 4개 확률과 좌우 로드엣지 Std 표시
- radarState의 leadsCenter/leadsLeft/leadsRight 전체 표시 (modelProb 필터 없음)
- liveTracks 감지점, ID, 거리·속도·센서·상태 표 추가
- 업데이트 시 기존 로그 자동 재변환

# 0.1.0
- 로그/영상 일괄 업로드, 영구 목록, 동기 재생과 삭제.
- 평면 파일명 및 구간 폴더 지원.
- Home Assistant Ingress 경로와 접근 제한 지원.
