# 설치 및 사용

현재 설명은 **0.2.85**의 기능을 기준으로 합니다.

## 설치와 접속

Home Assistant 앱 스토어의 저장소에 `https://github.com/Qualified4/roadviewer-ha`를 추가하고 **Road Viewer**를 설치·시작합니다. **웹 UI 열기** 또는 사이드바로 접속합니다. Home Assistant 버전에 따라 ‘앱’이 ‘애드온’으로 표시됩니다. HACS용 통합 구성요소가 아닙니다.

로컬 설치는 `roadviewer` 폴더를 Home Assistant의 `/addons/roadviewer`에 복사한 뒤 앱 스토어 목록을 새로고침해 설치할 수 있습니다. 첫 설치와 업데이트에서는 이미지를 빌드하고 필요한 패키지를 설치하므로 시간이 걸릴 수 있습니다.

원격 접속은 기존 Home Assistant 원격 주소로 로그인한 뒤 앱을 여세요. Ingress를 사용하므로 별도 Road Viewer 포트 개방은 필요하지 않습니다. Home Assistant Container에는 Supervisor 앱 스토어가 없으므로 이 방식으로 설치할 수 없습니다.

## 파일 업로드

**파일 추가**에서 파일을 선택하고 **업로드**를 누릅니다. 여러 파일을 한 번에 선택하거나 한 개씩 반복해서 추가할 수 있습니다. 업로드 전 **선택 비우기**로 선택을 지울 수 있습니다.

단일 구간은 다음 파일명을 사용합니다.

```text
rlog.zst
qcamera.ts
```

여러 구간은 아래처럼 루트 ID와 구간 번호가 포함된 파일명을 사용합니다. 여러 루트와 연속되지 않은 구간 번호도 함께 올릴 수 있습니다.

```text
0000035f--b513269850--0--rlog.zst
0000035f--b513269850--0--qcamera.ts
0000035f--b513269850--5--rlog.zst
0000035f--b513269850--5--qcamera.ts
```

- **로그만 업로드:** 변환 후 영상 없이 주행 상황·차량 정보를 재생할 수 있습니다.
- **영상 나중에 추가:** 기존과 동일한 로그 파일을 새 영상과 함께 선택해 업로드합니다. 기존 항목에 영상이 추가되고 변환 설정에 따라 다시 처리합니다.
- **영상만 업로드:** 등록할 수 없습니다. 짝이 되는 로그를 함께 선택하세요.
- **이미 영상이 있는 항목:** 새 영상으로 덮어쓰지 않습니다. 교체하려면 기존 항목을 완전 삭제하고 로그·영상을 다시 업로드합니다.

### 중복 판정

원본 `rlog.zst`의 SHA-256으로 비교합니다. 이름을 바꾼 동일 파일은 건너뛰고, 이름이 같아도 내용이 다르면 별도 로그로 등록합니다. 압축을 다시 만든 파일은 별개로 판정될 수 있습니다.

중복 판정은 전송 후 등록 단계에서 수행하므로 파일 전송은 발생하지만 중복 저장·변환은 하지 않습니다. 기존 로그에 없던 영상을 추가하는 경우는 예외입니다. 이미 저장된 중복 항목을 자동으로 삭제하지는 않습니다.

### 용량 제한과 모바일

| 항목 | 제한 또는 동작 |
|---|---|
| 한 번에 선택한 파일 수 | 최대 100개 |
| 한 번에 업로드할 파일의 합계 | 기본 512 MB, 앱 설정 `max_upload_mb`에서 16~2048 MB |
| 압축 해제된 단일 로그 | 최대 512 MB |
| 변환 작업 제한 시간 | 작업당 10분 |
| 업로드 전송 단위 | 256 KiB 조각, 일시적 전송 실패 시 최대 5회 재시도 |

`max_upload_mb`는 **업로드 한 번의 합계 제한**이며 전체 저장 용량 제한이 아닙니다. 화면의 MB 설정값은 내부에서 1024 단위로 계산합니다. 업로드 가능한 용량은 업로드 버튼 아래에 표시합니다.

Android Home Assistant 앱의 파일 선택창에서 다중 선택이 정상 반환되지 않으면 **파일 추가**를 반복해 한 개씩 선택할 수 있습니다. 폴더 선택과 파일 선택 진단 UI는 제공하지 않습니다. 업로드 중 앱을 닫거나 파일 접근 권한이 끊기면 실패할 수 있으며, 재접속 후에는 파일을 다시 선택해 업로드하세요. 실패한 업로드는 정리 대상이 됩니다.

## 로그 목록과 변환

목록과 재생 화면의 이름은 `00000395--0d0eda17c5--7`을 `395 / 구간 7`처럼 줄여 표시합니다. 옆 복사 아이콘은 선행 0과 해시를 포함한 원래 식별자 `00000395--0d0eda17c5--7`을 복사합니다. 저장 파일이나 구간 구분 기준은 바꾸지 않습니다.

상단의 저장공간은 Road Viewer가 사용하는 파일 용량의 합계입니다. 원본·변환 결과·업로드 임시 파일 등이 포함되며, 장치 전체 디스크의 사용량이나 남은 공간을 나타내지는 않습니다. 각 로그에는 보관 파일 용량(원본 로그·TS 또는 TS 대신 보관하는 MP4)과 제거 가능한 변환 데이터 용량을 따로 표시합니다. **로그 다운로드**는 ZST를 내려받습니다. **영상 다운로드**는 TS가 있으면 TS를, 없으면 보관 중인 MP4를 내려받습니다.

**영상 있음 / 영상 없음**은 보관 중인 TS 또는 MP4의 존재 여부입니다. MP4 변환 완료 여부와는 다릅니다. **재생** 버튼은 로그의 변환이 완료되면 표시되며 로그만 있는 항목에도 나타납니다.

### 변환 및 저장 설정

목록 상단에서 **자동 변환**을 켜거나 끌 수 있습니다. 켜면 대상 미변환 로그를 오래된 업로드 순서로 처리합니다. 끄면 대기 중인 자동 작업을 정리하지만, 이미 실행 중인 작업과 수동으로 요청한 변환은 계속 진행합니다.

**원본 영상 같이 보관**은 기본으로 켜져 있습니다. 켜면 TS와 재생용 MP4를 함께 보관합니다. 끄면 MP4 생성과 프레임 검증에 성공한 뒤 TS를 삭제하고 MP4만 보관합니다. 실패하거나 영상·로그 동기화가 불가능하면 TS를 유지합니다. 설정은 이후 완료되는 변환부터 적용되며 기존 파일은 설정 변경만으로 삭제되지 않습니다. 이미 삭제된 TS는 다시 켜도 복원되지 않습니다.

**동시 처리**는 1~4개 중 선택하며 기본은 1개입니다. 설정은 앱 재시작 후에도 유지됩니다. 개수를 낮춰도 실행 중인 작업은 중단하지 않고, 이후 새 작업을 시작할 때 변경한 한도를 적용합니다. 동시에 여러 개를 처리하면 완료 순서는 달라질 수 있습니다.

| 상태 | 의미와 사용 가능한 작업 |
|---|---|
| 미변환 | 로그와 보관 영상을 유지한 상태입니다. **변환**으로 재생 데이터를 만듭니다. |
| 대기 중 | 변환 순서를 기다립니다. 중복 변환 요청은 받지 않습니다. |
| 처리 중 | 로그 분석 또는 영상 변환 중입니다. 처리 단계와 가능한 진행 정보를 표시합니다. |
| 재생 가능 | **재생**하거나 **제거**로 변환 데이터만 지울 수 있습니다. |
| 변환 실패 | 오류를 확인하고 **변환**으로 다시 시도합니다. |

처리 중에는 로그 읽기·분석, 영상 읽기·변환·검증, 저장 단계가 표시됩니다. 로그 처리에서는 프레임 수, 영상 변환에서는 진행률 등을 표시하며 모든 단계가 백분율을 제공하지는 않습니다. 처리 진행 정보는 약 1초 간격으로 갱신됩니다.

### 로그 일괄 관리

변환 및 저장 설정 맨 아래 **로그 선택**을 누르면 일괄 관리 팝업이 열립니다. 상단의 **전체 선택**은 목록을 스크롤해도 고정되어 있습니다. 선택한 항목에 **변환**, **변환 데이터 제거**, **완전 삭제**를 적용합니다. 변환은 자동 변환이 꺼져 있어도 미변환·실패 항목을 수동 대기열에 넣으며, 설정한 동시 처리 개수를 따릅니다. 이미 대기·처리 중이거나 재생 가능한 항목은 건너뜁니다. 제거·완전 삭제 실행 전 선택 개수와 삭제 범위를 확인하며, 처리 중에는 선택과 닫기를 잠급니다. 완료 개수와 미완료 사유를 표시하고 미완료 항목만 선택을 유지합니다.

변환 데이터 제거는 보관 로그·TS 및 TS 없이 보관하는 MP4를 유지합니다. 대기·처리 중인 항목은 서버에서 거절하며 다른 항목은 계속 처리합니다. 자동 재변환하지 않으므로 이후 대상 로그를 다시 선택해 **변환**을 누릅니다. 완전 삭제는 보관 파일까지 모두 삭제합니다.

### 변환·제거·완전 삭제

목록에는 **재생** 또는 **변환** 버튼과 옆의 펼침 버튼(⌄)을 표시합니다. 펼침 메뉴에 **제거**, **로그 다운로드**, **영상 다운로드**, **완전 삭제**가 있습니다. 변환 전에는 **제거**를 숨기며 **완전 삭제**는 유지합니다. 대기·처리 중에는 바깥 버튼이 상태를 표시합니다.

- **변환:** TS에서 MP4를 만들고 로그를 분석합니다. MP4만 보관 중이면 영상을 재사용하며 분석·그래프 데이터를 만듭니다. 영상이 없으면 로그만 처리합니다.
- **제거:** 분석·그래프 및 TS에서 다시 만들 수 있는 MP4를 삭제합니다. 원본 로그·TS와 TS 대신 보관하는 유일한 MP4는 유지합니다. 수동 제거한 항목은 자동 변환이 켜져 있어도 다시 처리하지 않으며, **변환**을 직접 눌러야 합니다.
- **완전 삭제:** 원본과 변환 데이터를 모두 삭제합니다. 해당 항목의 진행 중인 작업도 취소합니다.

‘변환 및 저장 설정’는 로그 업로드 아래에서 접거나 펼칠 수 있으며, 접힘 상태를 브라우저에 기억합니다.

## 재생 화면과 배치

**로그 목록**, **이전 로그 / 다음 로그**, 같은 루트의 **구간 선택**으로 기록 사이를 이동합니다. 상단 제목·정보·탐색 영역은 스크롤 시 상단에 고정되며, 고정된 상태에서는 접기 버튼으로 탐색 버튼을 숨길 수 있습니다. 접힘 상태는 브라우저에 저장됩니다. 펼친 상태는 탐색 버튼 줄, 접힌 상태는 제목 줄이 상단에 닿을 때 고정되며, 위로 스크롤하면 탐색 버튼이 원래 위치에서 자연스럽게 다시 나타납니다.

하단 재생바는 스크롤해도 유지됩니다. 재생·일시정지, 이전·다음 프레임, 속도 조절(최대 4배), 시간 탐색을 지원합니다. 재생 중 탐색하면 옮긴 위치부터 계속 재생하고, 일시정지 중 탐색하면 일시정지를 유지합니다.

- **자동 배치:** 좌우 분할과 상하 분리를 모두 끄면 화면 크기에 따라 배치합니다.
- **좌우 분할:** 영상과 분석 패널을 나란히 표시합니다. 폭을 늘리면 영상과 주행 상황 높이도 함께 커집니다.
- **상하 분리:** 영상과 분석 패널을 위아래로 배치합니다.
- **화면 폭:** 좌우 분할 왼쪽 버튼을 눌러 팝업에서 720~1920 px의 최대 화면 폭을 조절합니다. 슬라이더 위치는 고정되고 결과는 즉시 반영됩니다. 화면 너비 600px 이하는 모바일(50~100%), 601~1280px은 태블릿(320~1280px), 그보다 넓으면 데스크톱(720~1920px) 범위를 사용합니다. 기본값은 모바일 100%, 태블릿 1280px, 데스크톱 1420px이며 각각 기억합니다. 실제 표시 폭은 화면 너비를 넘지 않습니다.

배치·화면 폭과 주요 표시 선택은 브라우저에 저장됩니다. 다른 기기나 브라우저에는 별도로 적용해야 합니다.

영상·주행 상황·차량 정보의 짧은 시작·종료 시차는 경계 허용 범위에서 보완합니다. 실제로 영상이 짧거나 로그가 끝난 구간을 임의로 늘려 재생하지는 않습니다. 차량 정보의 시작값을 미리 보여줄 때는 사용한 첫 유효 샘플 시각을 안내합니다.

## 주행 상황과 영상 겹쳐보기

주행 상황 패널에서 모델 주행 경로(`position`), 차선, 로드엣지, 모델 앞차, radarState 중앙·좌우 차량, liveTracks 감지점을 선택해 표시합니다. 좌우 폭과 전방 거리도 조절할 수 있습니다. **SCC 감지점 제외**를 켜면 주행 상황·영상 겹쳐 보기·liveTracks 표에서 SCC 출처 점을 숨깁니다. 모델 주행 경로의 초기값은 켜짐이며 이후 선택을 기억합니다.

**정보 표시**는 끄기·TrackID·거리·yRel·속도·상대 속도 중 하나를 고릅니다. 기본값은 TrackID이며, 끄면 도형만 남습니다. **liveTracks 정보 표시**를 켜면 선택한 정보를 원시 감지점에도 표시합니다. 모델 앞차의 확률이 50% 미만이면 흐리게 표시합니다.

전방 카메라 아래에서는 차선 4개의 확률과 좌우 로드엣지 표준편차를 확인합니다. **좌우 거리 y[0]**를 켜면 각 선 배열의 첫 번째 y값으로 전환합니다.

**주행 상황 겹쳐 보기**는 로그의 카메라 보정·센서 정보를 이용해 차선·모델 경로·감지 위치를 영상 위에 투영합니다. 필요한 정보가 없거나 유효하지 않으면 안내를 표시합니다. **차량 높이 조정**을 켜면 위치 표식을 설정한 높이로 표시하고 바닥까지 선을 연결합니다. 옆 설정 아이콘의 팝업에서 0~200cm를 1cm 단위로 조절합니다. 기본값은 60cm이며 0cm는 지면 위치입니다. 켜진 상태에서는 변경을 즉시 반영하고, 높이와 켜짐 상태는 각각 브라우저에 기억합니다. 높이 변경 자체로 로그를 다시 변환할 필요는 없습니다. 차량 모양이나 크기를 정확히 복원하는 기능은 아닙니다.

### 레이더 목록과 좌표

`radarState 차량 값`은 중앙·좌우 목록을 modelProb 필터 없이 표시합니다. `liveTracks 감지점`은 측정 여부와 관계없이 유효한 점을 표시합니다. 주행 상황의 **×는 measured=true**, 빈 원은 false입니다. 전방 카메라의 liveTracks 표식은 ×입니다. 원시 감지점이 모두 차량인 것은 아닙니다.

두 표는 약 10행 높이를 유지하며 나머지 항목은 내부 스크롤로 확인합니다. 열 제목은 고정되고, 감지 수가 줄거나 없어도 표 높이는 유지됩니다.

표의 `yRel`은 로그 원본 값이고 주행 상황에서는 `−yRel`로 변환합니다. 모델 좌표는 전방 x, 오른쪽 y를 사용합니다. 지도에는 선택한 범위 안의 점만 그리지만 표에는 범위 밖의 점도 포함합니다. 모델 앞차와 선택된 앞차의 거리는 기준점 차이가 있을 수 있습니다.

## 차량 정보 그래프

주행 상황 옆의 **차량 정보** 탭을 누르면 영상과 동기화된 그래프를 표시합니다. 상단에는 현재 속도·가속도·조향각·조향 개입을 요약합니다.

**그래프 선택** 팝업의 체크박스로 표시 항목을 고르고, 오른쪽 손잡이를 드래그해 순서를 바꿉니다. **순서 초기화**는 체크 상태를 유지한 채 기본 순서로 되돌립니다. 선택과 순서는 브라우저에 저장되며, 그래프가 많으면 그래프 영역 안에서 스크롤합니다.

기본 순서의 20개 그래프는 다음과 같습니다.

| 그룹 | 그래프 |
|---|---|
| 속도·가감속 | 속도, 가속도, 자동 목표 가속도, 자동 가감속 저크, 회전수(RPM) |
| 페달·제동 | 운전자 페달 입력량, 운전자 페달 조작 상태, 자동 가스·브레이크 출력, 자동 가속·감속 요청, 자동 가감속 제어 단계, 정차·브레이크 홀드 |
| 조향 | 조향각, 핸들 회전 속도, 조향 토크, 자동 조향 토크 명령, 운전자 조향 개입, 주행 곡률, 횡가속도 제어 |
| 제어 상태 | 자동 제어 상태, 크루즈 상태 |

처음에는 속도·가속도·운전자 페달 입력량·조향각·운전자 조향 개입·자동 제어 상태를 표시합니다.

`+ / −`로 시간축을 확대·축소하고 **전체 구간**으로 되돌립니다. 그래프 클릭·가로 드래그로 재생 위치를 옮길 수 있습니다. 확대 상태에서는 가능한 범위 안에서 현재 시각이 중앙에 오도록 따라가며, 드래그 중에는 시간축을 유지하고 놓으면 다시 중앙으로 맞춥니다.

### 값 해석

- 운전자 페달 입력량·밟음 여부와 자동 가감속 명령·최종 출력은 별개입니다. 브레이크 밟음 여부만 기록하고 입력량은 채우지 않는 차량도 있습니다.
- 자동 감속 요청은 요청 가속도의 부호를 표시하며 실제 브레이크 페달 조작을 뜻하지 않습니다.
- 조향 토크 그래프의 ‘운전자’는 `carState.steeringTorque`의 센서값입니다. 자동 조향 명령과 다르며, 값의 변화만으로 운전자 개입을 확정하지 않습니다. 조향 개입 상태는 `steeringPressed`를 사용합니다.
- 조향각 목표는 제어기에 기록된 값입니다. 토크 제어 차량의 목표각도 포함하지만 모든 차량에서 기록되는 것은 아닙니다. RPM 역시 차량 구현에 따라 제공 여부와 의미가 다릅니다.
- 로그 전체에서 해당 항목의 유효한 값이 모두 0이면 실제 0과 미기록 기본값을 구분할 수 없다는 안내가 표시됩니다. 유효한 기록이 없으면 별도로 안내합니다. 없는 값을 임의로 추정해 채우지 않습니다.

## 카메라 설치 정보와 핸들 표시

**카메라 설치 정보**는 버튼을 누른 시점의 기기 종류, 디바이스 ID(`dongleId`), 센서, 보정 상태, 롤·피치·요와 카메라 높이를 표시합니다. ID가 없으면 확인 불가로 표시합니다. 높이는 로그 보정값 또는 기본값 사용 여부를 구분하고, 좌우·앞뒤 설치 오프셋은 확인 불가로 표시합니다.

각도는 차 안에서 전방을 바라볼 때의 카메라 설치 방향으로 해석합니다.

| 각도 | − 방향 | + 방향 |
|---|---|---|
| 롤 | 오른쪽 기울기·시계 방향 | 왼쪽 기울기·반시계 방향 |
| 피치 | 위쪽 | 아래쪽 |
| 요 | 오른쪽 | 왼쪽 |

기본 보정은 롤을 0으로 가정하므로 `0°`가 실제 수평을 보장하지는 않습니다. 이 설명은 팝업에도 표시됩니다.

카메라 상단 속도는 `carState.vEgo` 기반이며 계기판 속도와 다를 수 있습니다. 핸들 아이콘은 기록된 조향각으로 회전하고, `steeringPressed`가 참이면 흰색으로 운전자 조향 개입을 표시합니다. 자동 조향 상태·토크에 따라 색상과 크기가 바뀌며 유효한 상태가 없으면 확인 불가로 표시합니다. 손을 단순히 얹었는지 또는 페달을 조작했는지 판정하는 표시가 아닙니다.

## 저장소 정리·백업·업데이트

앱 내부 저장 경로는 `/data/roadviewer`입니다. 로그별 폴더에 원본과 변환 결과를 보관합니다. 이는 앱 컨테이너 내부 경로이며 Home Assistant 호스트에서 보이는 경로와 같다고 가정하면 안 됩니다.

미완료 업로드는 1분마다 검사하며, 15분 이상 전송이 없으면 정리합니다. 업로드 영역은 접을 수 있으며 접힘 상태를 기억합니다. **저장소 즉시 정리**는 업로드 영역에서 누를 수 있으며, 15분이 지나지 않았어도 미완료 임시 업로드를 삭제합니다. 등록을 마무리 중인 파일과 등록된 로그는 보존하지만, 현재 업로드는 중단하며 다른 기기의 업로드도 중단될 수 있습니다.

Home Assistant 앱 백업에서는 다음을 제외합니다.

- 원본 TS 영상과 rlog.zst 로그
- 변환 MP4, 분석·그래프 JSON, 로그별 목록 메타데이터
- 업로드 임시 파일과 이전 파일 선택 진단 데이터

앱 옵션(`/data/options.json`)과 처리 설정(`/data/roadviewer/.processing-settings.json`)은 백업에 유지합니다. **백업 복원 후 기록 목록은 비어 있으므로 보관한 원본을 다시 업로드해야 합니다.** 이미 만들어진 과거 백업 파일의 내용은 바뀌지 않습니다. 브라우저에 저장한 표시 설정은 앱 백업 대상이 아닙니다.

앱 재시작 시 중단된 작업의 불완전한 변환 결과를 정리하고 변환 설정에 따라 다시 대기열에 넣습니다. 수동으로 요청한 변환은 자동 변환이 꺼져 있어도 재개 대상입니다. 로그 분석 형식이 바뀌는 업데이트에서는 대상 기존 변환 결과를 먼저 정리한 뒤 오래된 업로드부터 다시 처리합니다. 자동 변환을 껐거나 수동 제거한 항목은 수동 변환이 필요합니다. 원본 로그·TS와 TS 대신 보관하는 MP4는 유지합니다.

## 문제 확인과 검증 범위

업로드 실패 시 파일 접근 상태·네트워크·저장공간을 확인하세요. 앱 로그의 `Upload failure`에는 실패 단계와 전송 위치 등 진단 정보가 남습니다. 변환 실패는 목록에 표시되는 오류와 앱 로그를 확인한 뒤 다시 변환할 수 있습니다.

GitHub Actions에는 amd64/aarch64 빌드, 이미지 메타데이터, API·업로드·변환·스키마 관련 검사와 브라우저 테스트가 구성되어 있습니다. 실제 Home Assistant 장비의 처리 성능과 모바일 네이티브 파일 선택창은 환경에 따라 다릅니다.

포함된 cereal/car 스키마는 사용 중인 openpilot 체크아웃을 기반으로 합니다. 다른 브랜치의 로그 형식과 차이가 크면 스키마 갱신이 필요할 수 있습니다. 스키마와 UI 리소스의 라이선스는 [OPENPILOT-LICENSE](schema/OPENPILOT-LICENSE)를 참고하세요.
