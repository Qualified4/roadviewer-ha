# 설치 및 사용

## Home Assistant OS에 로컬 앱으로 설치

1. 이 패키지의 `roadviewer` 폴더 전체를 Home Assistant의 `/addons/roadviewer`에 복사합니다. Home Assistant의 Samba 또는 SSH 앱 등 기존 파일 전송 수단을 사용합니다.
2. Home Assistant의 설정 → 앱 스토어에서 목록을 새로고침합니다. 버전에 따라 메뉴 이름은 애드온 스토어입니다.
3. 로컬 앱의 Road Viewer를 설치하고 시작합니다. 첫 설치에서는 Python 패키지를 다운로드해 이미지를 빌드합니다.
4. 웹 UI 열기 또는 사이드바 표시를 사용합니다.

앱에는 호스트 네트워크, USB, 장치 제어, Supervisor API 권한이 필요하지 않습니다. 원격에서는 기존 Home Assistant 원격 접속 주소로 로그인한 뒤 Road Viewer를 여세요. Ingress가 인증을 처리하며 앱은 Supervisor의 Ingress 주소에서 들어오는 요청만 허용합니다.

Home Assistant Container 설치에는 Supervisor 앱 스토어가 없습니다. 이 패키지를 앱으로 설치할 수 없으므로 인증이 있는 리버스 프록시와 별도 컨테이너 배포 구성이 필요합니다. 제공된 기본 이미지를 공개 포트에 직접 노출하는 구성은 포함하지 않습니다.

## 업로드

파일 선택 또는 폴더 선택을 누르고 업로드합니다. 업로드 진척률과 변환 상태가 표시됩니다. 변환이 끝나면 목록의 재생 버튼이 활성화됩니다.

같은 구간의 파일은 함께 올려주세요.

```
rlog.zst
qcamera.ts
```

여러 구간은 아래 파일명을 지원합니다.

```
0000035f--b513269850--0--rlog.zst
0000035f--b513269850--0--qcamera.ts
0000035f--b513269850--5--rlog.zst
0000035f--b513269850--5--qcamera.ts
```

여러 루트 혼합 및 누락된 구간 번호를 허용합니다. `qcamera.ts`가 없으면 도로 평면도만 표시합니다. 영상만 올리는 경우 짝이 되는 로그를 함께 선택하라는 오류를 표시합니다. 같은 로그를 다시 업로드하면 별도 항목으로 보관됩니다. 한 번에 최대 100개 파일을 업로드합니다.

파일은 1MiB씩 나누어 전송하여 Home Assistant Ingress의 요청 크기 제한에 대응합니다. `max_upload_mb`는 한 번에 선택한 전체 파일 크기 제한이며 기본 512MB입니다. 16~2048MB로 변경할 수 있습니다. 압축 해제된 단일 로그는 최대 512MB, 변환 작업은 최대 10분으로 제한합니다. 사용하는 원격 프록시에도 업로드 크기 제한이 있다면 해당 설정이 함께 적용됩니다.

## 목록·재생·삭제

- 목록의 대기 중/준비 중 상태는 자동으로 갱신됩니다.
- 재생을 누르면 영상과 차량 중심 도로 지도가 열립니다. 재생바, 이전/다음, 재생 속도, 차선/로드엣지/앞차 표시를 조절할 수 있습니다.
- 삭제를 누르고 확인하면 해당 항목의 원본 로그, 영상과 변환 파일을 모두 삭제합니다. 준비 중인 변환도 취소합니다.
- 앱 재시작 후에도 목록과 업로드 파일은 `/data/roadviewer`에 보관됩니다. 중단된 변환은 다시 대기열에 넣습니다. 앱을 제거할 때 데이터도 제거될 수 있으므로 보존이 필요하면 Home Assistant 백업을 사용하세요.

모델 좌표의 전방 x, 오른쪽 y를 사용합니다. 모델의 추정이며 실제 측량값이 아닙니다. 모델 앞차 거리와 선택된 앞차 거리는 기준점 차이가 있을 수 있습니다. 영상 위의 오버레이가 아니라 영상 옆의 평면도입니다.

## 검증 범위

amd64 Docker 이미지 빌드, 실제 로그 변환, 업로드·목록·재생·삭제 API, 영상 Range 응답, 잘못된 업로드, Ingress 허용 주소 검사를 테스트했습니다. Windows Edge에서 Ingress와 같은 하위 URL 경로의 업로드·재생·삭제, 좁은 화면 레이아웃을 테스트했습니다. 실제 사용자의 Home Assistant 인스턴스 설치와 원격 접속은 설치 환경 확인 후 검증해야 합니다. aarch64는 선언되어 있으나 실기기 검증 전입니다.

## 스키마

앱에 포함한 cereal/car 스키마는 이 작업에 사용한 openpilot 체크아웃에서 가져왔습니다. 로그 형식이 변경된 차량 브랜치에는 스키마 갱신이 필요할 수 있습니다. `schema/OPENPILOT-LICENSE`를 함께 배포합니다.

## 0.2.0 표시 기능

차선 4개의 확률과 좌우 로드엣지 Std를 표시합니다. `radarState` 중앙·좌우 목록은 modelProb 필터 없이 표시합니다. `liveTracks 감지점`은 측정 여부와 관계없이 유효한 좌표의 감지점을 표시하며, +는 measured=true, 빈 원은 false입니다. 감지점 ID 옵션과 아래 표에서 거리·횡방향 위치·상대속도·센서·상태를 확인하세요. 표의 yRel은 원본 값이고 지도에서는 -yRel로 변환합니다. 지도에는 선택한 범위 안의 점만 표시하며 표에는 범위 밖의 점도 포함합니다. 원시 감지점이 모두 차량인 것은 아닙니다.

기존 0.1.0에서 업데이트하면 저장된 원본 로그를 최신 표시 형식으로 자동 재변환합니다.

## 내 차량 속도

카메라 상단에 carState.vEgo × 3.6 값을 km/h로 표시합니다. 계기판 표시 속도와 차이가 있을 수 있습니다. 영상 프레임 시각과 가장 가까운 carState를 사용하며, 유효하지 않거나 150ms 이상 떨어진 값은 —로 표시합니다. 업데이트 시 기존 로그도 자동 재변환됩니다.


## 조향 핸들 표시 (0.2.5)

속도 옆 원본 mici 당근 핸들이 carState.steeringAngleDeg의 음수 각도로 회전합니다. carState.steeringPressed가 참이면 운전자 조향 개입으로 흰색을 우선 적용합니다. 이는 조향 개입 신호이며 가속·브레이크 개입이나 손을 단순히 얹었는지를 판정하지 않습니다. carControl.latActive가 참이면 토크에 따라 원본 HSV 녹색→주황색 및 1~1.5배 확대를 적용합니다. 비활성은 원본 밝은 회색, 필수 상태 누락·무효·150ms 경과는 확인 불가로 표시합니다. 원본 휠 PNG와 틴트 없는 중앙 당근 이미지를 사용합니다.

토크는 carOutput.actuatorsOutput.torque를 사용하며 angleState는 desiredCurvature, vEgo, liveParameters.roll, carParams.maxLateralAccel로 원본 보상식을 계산합니다. 0.1초 저역통과 필터는 재생 프레임의 로그 시간 간격을 사용하므로 기기의 GUI 프레임 속도와 차이가 날 수 있습니다. liveParameters는 2초, carParams는 마지막 유효 설정을 사용합니다. 토크가 없으면 기본 크기로 표시합니다.

controlsState.activeLaneLine으로 레인 이미지를 표시합니다. selfdriveState.alertHudVisual=steerRequired 및 표시 중인 alertSize로 위험 휠과 느낌표를 표시합니다. 원본 UI의 경고 퇴장 애니메이션·차선 변경 의도 애니메이션은 재현하지 않습니다. 기존 업로드는 업데이트 시 자동 재변환됩니다.

아이콘 출처: 사용자 지정 openpilot selfdrive/assets/icons_mici 및 selfdrive/ui/mici/onroad/hud_renderer.py. 원본 라이선스는 schema/OPENPILOT-LICENSE에 포함됩니다.
